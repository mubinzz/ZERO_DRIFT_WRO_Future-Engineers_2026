# Raspberry Pi 5 এ ইনস্টল করার প্যাকেজ লিস্ট
#
# ইনস্টল করার নিয়ম (Raspberry Pi OS Bookworm এ):
#   sudo apt update
#   sudo apt install python3-opencv python3-picamera2 python3-gpiozero
#   pip install --break-system-packages -r requirements.txt
#
# ব্যাখ্যা: opencv আর picamera2 apt দিয়ে ইনস্টল করা ভালো কারণ এগুলো
# libcamera/system লাইব্রেরির উপর নির্ভর করে, pip দিয়ে করলে অনেক সময়
# ভার্সন মিলে না। বাকিগুলো pip দিয়ে ইনস্টল করলেই যথেষ্ট।

pyserial>=3.5      # ESP32 এর সাথে USB serial communication এর জন্য
rplidar>=0.9.1      # RPLIDAR A3M1 এর সাথে কথা বলার জন্য (PyPI তে সর্বোচ্চ ভার্সন 0.9.2)
numpy>=1.24         # numeric computation (LIDAR পয়েন্ট, PID হিসাব ইত্যাদি)
